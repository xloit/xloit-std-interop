/**
 * Constructor to prevent {@link ${CLASS_NAME}} from being loaded more than once.
 *
#parse("Since.php")
#parse("Author.php")
 *
${PARAM_DOC}
 *
 * @return self
 */
public function __construct(${PARAM_LIST}) {
    ${BODY}
}